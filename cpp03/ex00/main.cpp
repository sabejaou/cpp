#include "ClapTrap.hpp"

int main( void )
{
    ClapTrap a("Robert");
    ClapTrap b("Bob");

    a.attack(b.getname());
    a.attack(b.getname());
    a.attack(b.getname());
    a.attack(b.getname());
    a.attack(b.getname());
    a.attack(b.getname());
    a.attack(b.getname());
    a.attack(b.getname());
    a.attack(b.getname());
    a.attack(b.getname());
    a.attack(b.getname());
}